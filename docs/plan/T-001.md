# T-001: План архитектуры MVP

## Решения по открытым вопросам

| Вопрос | Решение |
|--------|---------|
| Подтверждение записи | Не нужно. Запись открывается автоматически утром за день до события |
| Переизбыток записавшихся | Мягкое ограничение. Записаться могут все, тренер выбирает состав |
| История событий | Показываем прошедшие события |
| Роль капитана | Не нужна в MVP |
| Статусы записи | Только GOING / NOT_GOING (без MAYBE) |
| Invite code | Случайный код, без модерации, с возможностью перегенерировать |
| Real-time обновления | Polling (refetch при фокусе на странице) |
| Уведомления | В чат команды через Telegram бота |

## Важные бизнес-правила

- **Все роли могут записываться на события** — тренер и админ тоже игроки
- **Событие видно сразу после создания**, но кнопка "Записаться" появляется только когда `registration_opens_at <= NOW()`
- **Запись открывается автоматически** утром за день до события

## Обзор архитектуры

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│                 │     │                 │     │                 │
│  Telegram Bot   │────▶│  Ktor Backend   │────▶│   PostgreSQL    │
│  (уведомления)  │     │     (API)       │     │                 │
│                 │     │                 │     │                 │
└─────────────────┘     └────────▲────────┘     └─────────────────┘
                                 │
                        ┌────────┴────────┐
                        │                 │
                        │   React App     │
                        │  (Mini App)     │
                        │                 │
                        └─────────────────┘
```

## База данных

### Схема

```sql
-- Пользователи (из Telegram)
CREATE TABLE users (
    id BIGSERIAL PRIMARY KEY,
    telegram_id BIGINT UNIQUE NOT NULL,
    first_name VARCHAR(255) NOT NULL,
    last_name VARCHAR(255),
    username VARCHAR(255),
    created_at TIMESTAMP DEFAULT NOW()
);

-- Команды
CREATE TABLE teams (
    id BIGSERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    invite_code VARCHAR(8) UNIQUE NOT NULL,
    telegram_chat_id BIGINT,  -- чат для уведомлений (nullable до привязки)
    created_at TIMESTAMP DEFAULT NOW()
);

-- Членство в команде
CREATE TABLE team_members (
    id BIGSERIAL PRIMARY KEY,
    user_id BIGINT REFERENCES users(id) ON DELETE CASCADE,
    team_id BIGINT REFERENCES teams(id) ON DELETE CASCADE,
    role VARCHAR(20) NOT NULL CHECK (role IN ('PLAYER', 'COACH', 'ADMIN')),
    joined_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(user_id, team_id)
);

-- События
CREATE TABLE events (
    id BIGSERIAL PRIMARY KEY,
    team_id BIGINT REFERENCES teams(id) ON DELETE CASCADE,
    type VARCHAR(20) NOT NULL CHECK (type IN ('GAME', 'TRAINING')),
    title VARCHAR(255),
    description TEXT,
    event_date DATE NOT NULL,
    event_time TIME NOT NULL,
    location VARCHAR(255),
    max_players INT,
    registration_opens_at TIMESTAMP NOT NULL,  -- когда открывается запись
    status VARCHAR(20) NOT NULL DEFAULT 'SCHEDULED'
        CHECK (status IN ('SCHEDULED', 'OPEN', 'CLOSED', 'CANCELLED')),
    created_by BIGINT REFERENCES users(id),
    created_at TIMESTAMP DEFAULT NOW()
);

-- Записи на события
CREATE TABLE registrations (
    id BIGSERIAL PRIMARY KEY,
    event_id BIGINT REFERENCES events(id) ON DELETE CASCADE,
    user_id BIGINT REFERENCES users(id) ON DELETE CASCADE,
    status VARCHAR(20) NOT NULL CHECK (status IN ('GOING', 'NOT_GOING')),
    registered_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(event_id, user_id)
);

-- Состав на игру/тренировку
-- Для игры line_group: LINE_1, LINE_2, LINE_3, LINE_4, GOALIES
-- Для тренировки line_group: LIGHT_1, LIGHT_2, LIGHT_GOALIES, DARK_1, DARK_2, DARK_GOALIES
CREATE TABLE lineups (
    id BIGSERIAL PRIMARY KEY,
    event_id BIGINT REFERENCES events(id) ON DELETE CASCADE,
    user_id BIGINT REFERENCES users(id) ON DELETE CASCADE,
    line_group VARCHAR(20) NOT NULL,
    position VARCHAR(10),  -- LW, C, RW, LD, RD, G (NULL если не назначена)
    created_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(event_id, user_id)
);

-- Индексы
CREATE INDEX idx_events_team_date ON events(team_id, event_date);
CREATE INDEX idx_events_registration_opens ON events(registration_opens_at) WHERE status = 'SCHEDULED';
CREATE INDEX idx_registrations_event ON registrations(event_id);
CREATE INDEX idx_lineups_event ON lineups(event_id);
CREATE INDEX idx_team_members_user ON team_members(user_id);
```

### Статусы события

```
SCHEDULED → OPEN → CLOSED → (событие прошло)
    │         │
    └─────────┴────→ CANCELLED
```

- **SCHEDULED** — событие создано, запись ещё не открыта
- **OPEN** — запись открыта (автоматически в `registration_opens_at`)
- **CLOSED** — тренер закрыл запись / сформировал состав
- **CANCELLED** — событие отменено

## Backend API

### Авторизация

Telegram Mini App передаёт `initData` в заголовке `Authorization`.
Backend валидирует HMAC-подпись с использованием токена бота.

```
Authorization: tma <initData>
```

### Endpoints

#### Auth
```
POST /api/auth/init
  Body: { initData: string }
  Response: { user: User, teams: Team[] }
```

#### Teams
```
GET    /api/teams                         -- мои команды
POST   /api/teams                         -- создать команду (становишься ADMIN)
GET    /api/teams/{id}                    -- информация о команде
POST   /api/teams/join                    -- присоединиться по коду
DELETE /api/teams/{id}/leave              -- покинуть команду
POST   /api/teams/{id}/regenerate-code    -- перегенерировать invite code (ADMIN)
GET    /api/teams/{id}/members            -- список участников
PATCH  /api/teams/{id}/members/{userId}   -- изменить роль (ADMIN)
DELETE /api/teams/{id}/members/{userId}   -- удалить из команды (ADMIN)
```

#### Events
```
GET    /api/teams/{teamId}/events              -- список событий команды
POST   /api/teams/{teamId}/events              -- создать событие (COACH/ADMIN)
GET    /api/teams/{teamId}/events/{id}         -- детали события
PATCH  /api/teams/{teamId}/events/{id}         -- редактировать (COACH/ADMIN)
DELETE /api/teams/{teamId}/events/{id}         -- отменить (COACH/ADMIN)
POST   /api/teams/{teamId}/events/{id}/close   -- закрыть запись (COACH/ADMIN)
```

#### Registrations
```
GET    /api/events/{eventId}/registrations     -- кто записался
POST   /api/events/{eventId}/registrations     -- записаться { status: GOING | NOT_GOING }
DELETE /api/events/{eventId}/registrations     -- удалить свою запись
```

#### Lineup
```
GET    /api/events/{eventId}/lineup            -- текущий состав
PUT    /api/events/{eventId}/lineup            -- сохранить состав (COACH/ADMIN)
       Body: { entries: [{ userId, lineGroup, position? }] }
```

### Формат состава

**Игра (3-4 пятёрки + вратари):**
```
LINE_1:  LW, C, RW, LD, RD
LINE_2:  LW, C, RW, LD, RD
LINE_3:  LW, C, RW, LD, RD
LINE_4:  LW, C, RW, LD, RD  (опционально)
GOALIES: G, G
```

**Тренировка (2 команды по 2 пятёрки):**
```
LIGHT_1: LW, C, RW, LD, RD
LIGHT_2: LW, C, RW, LD, RD
LIGHT_GOALIES: G

DARK_1:  LW, C, RW, LD, RD
DARK_2:  LW, C, RW, LD, RD
DARK_GOALIES: G
```

### Структура ответов

```json
// Успешный ответ
{
    "data": { ... }
}

// Ошибка
{
    "error": {
        "code": "REGISTRATION_NOT_OPEN",
        "message": "Запись на это событие ещё не открыта"
    }
}
```

## Frontend

### Страницы

```
/                       -- список моих команд (или редирект если одна)
/join?code=XXX          -- присоединиться к команде
/teams/new              -- создать команду
/teams/{id}             -- события команды (главный экран)
/teams/{id}/members     -- участники команды
/teams/{id}/settings    -- настройки команды (для ADMIN)
/events/{id}            -- детали события + запись
/events/{id}/lineup     -- формирование состава (для COACH/ADMIN)
```

### Ключевые компоненты

```
components/
├── EventCard.tsx           -- карточка события в списке
├── EventDetails.tsx        -- детали события
├── RegistrationButton.tsx  -- кнопка "Иду" / "Не иду"
├── RegistrationList.tsx    -- список записавшихся
├── LineupEditor.tsx        -- редактор состава (drag-n-drop)
├── MembersList.tsx         -- список участников команды
└── InviteCode.tsx          -- показ/копирование/перегенерация кода
```

### Состояние

React Query для кэширования и синхронизации:

```typescript
// Refetch при фокусе на странице
const queryClient = new QueryClient({
    defaultOptions: {
        queries: {
            refetchOnWindowFocus: true,
            staleTime: 30_000, // 30 секунд
        },
    },
});
```

## Telegram Bot

### Команды

```
/start              -- приветствие + кнопка открыть Mini App
/start {inviteCode} -- deep link для присоединения к команде
/connect            -- привязать текущий чат к команде (отправляется в групповой чат)
```

### Привязка чата к команде

1. Админ добавляет бота в групповой чат команды
2. Админ пишет `/connect INVITE_CODE`
3. Бот проверяет, что отправитель — ADMIN этой команды
4. Бот сохраняет `chat_id` в таблицу `teams`
5. Теперь бот может слать уведомления в этот чат

### Уведомления в чат

| Событие | Сообщение |
|---------|-----------|
| Новое событие | 🏒 **Игра**: 15 января, 20:00, Арена "Лёд" |
| Запись открыта | 📝 Открыта запись на игру 15 января! [Записаться] |
| Состав сформирован | ✅ Состав на игру 15 января сформирован. Основа: 12 чел. [Посмотреть] |
| Событие отменено | ❌ Игра 15 января отменена |

### Реализация

- **Long polling** для локальной разработки
- Inline-кнопки со ссылками на Mini App

## Автоматическое открытие записи

Корутина в Ktor, запускается при старте приложения:

```kotlin
fun Application.scheduleRegistrationOpener() {
    launch {
        while (isActive) {
            val opened = eventService.openScheduledRegistrations()
            opened.forEach { event ->
                telegramBot.notifyRegistrationOpen(event)
            }
            delay(5.minutes)  // проверяем каждые 5 минут
        }
    }
}
```

## Безопасность

### Права доступа

| Действие | PLAYER | COACH | ADMIN |
|----------|--------|-------|-------|
| Просмотр событий | ✅ | ✅ | ✅ |
| Запись на событие | ✅ | ✅ | ✅ |
| Создание события | ❌ | ✅ | ✅ |
| Редактирование события | ❌ | ✅ | ✅ |
| Формирование состава | ❌ | ✅ | ✅ |
| Управление участниками | ❌ | ❌ | ✅ |
| Изменение ролей | ❌ | ❌ | ✅ |
| Перегенерация invite code | ❌ | ❌ | ✅ |
| Привязка чата (/connect) | ❌ | ❌ | ✅ |

## Структура проекта

### Backend

```
backend/src/main/kotlin/com/smena/
├── Application.kt
├── plugins/
│   ├── Authentication.kt
│   ├── ContentNegotiation.kt
│   ├── CORS.kt
│   ├── Routing.kt
│   └── StatusPages.kt
├── routes/
│   ├── AuthRoutes.kt
│   ├── TeamRoutes.kt
│   ├── EventRoutes.kt
│   ├── RegistrationRoutes.kt
│   └── LineupRoutes.kt
├── services/
│   ├── AuthService.kt
│   ├── TeamService.kt
│   ├── EventService.kt
│   ├── RegistrationService.kt
│   └── LineupService.kt
├── repositories/
│   ├── UserRepository.kt
│   ├── TeamRepository.kt
│   ├── EventRepository.kt
│   ├── RegistrationRepository.kt
│   └── LineupRepository.kt
├── models/
│   ├── User.kt
│   ├── Team.kt
│   ├── Event.kt
│   ├── Registration.kt
│   └── Lineup.kt
├── dto/
│   ├── CreateTeamRequest.kt
│   ├── CreateEventRequest.kt
│   └── ...
├── telegram/
│   ├── Bot.kt
│   └── InitDataValidator.kt
└── db/
    └── DatabaseFactory.kt
```

### Frontend

```
frontend/src/
├── main.tsx
├── App.tsx
├── api/
│   ├── client.ts
│   ├── auth.ts
│   ├── teams.ts
│   ├── events.ts
│   └── registrations.ts
├── components/
│   ├── ui/
│   └── ...
├── pages/
│   ├── TeamsPage.tsx
│   ├── TeamPage.tsx
│   ├── EventPage.tsx
│   └── LineupPage.tsx
├── hooks/
│   ├── useAuth.ts
│   ├── useTeams.ts
│   └── useEvents.ts
├── types/
│   └── index.ts
└── utils/
    └── telegram.ts
```

## Зависимости

### Backend

```kotlin
// Ktor 3.x
implementation("io.ktor:ktor-server-core")
implementation("io.ktor:ktor-server-netty")
implementation("io.ktor:ktor-server-content-negotiation")
implementation("io.ktor:ktor-serialization-kotlinx-json")
implementation("io.ktor:ktor-server-cors")
implementation("io.ktor:ktor-server-status-pages")

// Exposed 0.58+
implementation("org.jetbrains.exposed:exposed-core")
implementation("org.jetbrains.exposed:exposed-jdbc")
implementation("org.jetbrains.exposed:exposed-java-time")

// PostgreSQL
implementation("org.postgresql:postgresql")
implementation("com.zaxxer:HikariCP")

// Telegram
implementation("io.github.kotlin-telegram-bot.kotlin-telegram-bot:telegram")

// Logging
implementation("ch.qos.logback:logback-classic")
```

### Frontend

```json
{
    "dependencies": {
        "react": "^19",
        "react-dom": "^19",
        "react-router-dom": "^7",
        "@tanstack/react-query": "^5",
        "@telegram-apps/sdk-react": "^2",
        "axios": "^1"
    },
    "devDependencies": {
        "typescript": "~5.9",
        "vite": "^7",
        "tailwindcss": "^4",
        "@types/react": "^19"
    }
}
```

## Порядок реализации

### Фаза 1: Скелет проекта
1. Gradle проект с зависимостями
2. Vite + React + TypeScript проект
3. Базовый Application.kt с плагинами
4. Подключение к БД и миграции

### Фаза 2: Авторизация
6. Telegram initData валидация
7. Endpoint POST /api/auth/init
8. Middleware проверки авторизации

### Фаза 3: Команды
9. CRUD команд
10. Присоединение по invite code
11. Перегенерация invite code
12. Управление участниками

### Фаза 4: События
13. CRUD событий
14. Фоновая задача открытия записи
15. Запись на события (GOING / NOT_GOING)

### Фаза 5: Состав
16. Сохранение/получение состава

### Фаза 6: Telegram Bot
17. /start с кнопкой Mini App
18. /connect для привязки чата
19. Уведомления в чат

### Фаза 7: Frontend
20. Telegram SDK интеграция
21. Авторизация
22. Список команд / создание / вступление
23. Список событий
24. Страница события с записью
25. Редактор состава
